<h1>Add a page</h1>

<% form_for :page, @page, :url => admin_pages_path, :html => { :multipart => true } do |f| -%>
  <%= render :partial => 'form', :locals => { :f => f } %>
	<p><%= f.submit('Save') %> -or- <%= link_to('cancel', admin_pages_path) %></p>
<% end -%>